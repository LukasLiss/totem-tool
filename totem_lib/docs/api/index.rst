API reference
=============

Everything here can be imported from ``totem_lib`` directly, for example
``from totem_lib import import_ocel``. The only exceptions are the two DuckDB
entry points on the event logs page, which live in ``totem_lib.ocel``.

.. toctree::
   :maxdepth: 1

   event_logs
   totem
   process_areas
   dfg
   ocpn
   occn
   variants
   playout
